{
  "name": "@superwindcloud/bevy-runeweave",
  "version": "0.1.0",
  "author": "superwindcloud",
  "private": true,
  "scripts": {
    "build": "esbuild src/shooter.ts --bundle --format=iife --target=es2023 --outfile=assets/shooter.js",
    "watch": "esbuild src/shooter.ts --bundle --format=iife --target=es2023 --outfile=assets/shooter.js --watch",
    "check": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "7.0.2"
  }
}
