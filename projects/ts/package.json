{
  "name": "script-squadron-typescript-scripts",
  "version": "0.1.0",
  "author": "superwindcloud",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "watch": "tsc --project tsconfig.json --watch --preserveWatchOutput",
    "check": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
